# Lesson-writing skill

This package supports the Learn stage. It turns the completed workflow and its evaluator feedback into one useful rule that can improve a later related run.

`SKILL.md` requires the node to recall existing lessons, avoid duplicates, produce exactly one generalizable rule, and exclude secrets, absolute paths, and run-specific details. The workflow scrubs the result before it is stored.
